apiVersion: v1
kind: Service
metadata:
  name: liferay
spec:
  selector:
    app: liferay
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080  # You can change this port if needed
